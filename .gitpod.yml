---
image:
  file: .gitpod.Dockerfile

tasks:
  - name: Build the documentation
    init: |
      mkdir _build
      cd _build
      ../autogen.sh --prefix=${INSTALL_PREFIX} --without-gimp
      make
